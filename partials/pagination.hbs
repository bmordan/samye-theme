<nav id="pagination" role="pagination">
  {{#if pagination.prev}}
  <a class="newer-posts" href="/page/{{pagination.prev}}/">←</a>
  {{/if}}
  {{#if pagination.next}}
  <a class="older-posts" href="/page/{{pagination.next}}/">→</a>
  {{/if}}
</nav>